[pytest]
# add the repo root onto PYTHONPATH so `import src.amazon_recommender…` can find your package
pythonpath = .

# where to look for tests
testpaths = tests

# (optional) only collect test_*.py
# python_files = test_*.py
